REACT_APP_BACKEND_URL=https://your-backend.railway.app
# Note: Replace with your actual Flask backend Railway URL (without trailing /api)
# Create .env file from this template (add to .gitignore)
# Then npm run build && deploy
